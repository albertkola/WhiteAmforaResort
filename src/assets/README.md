# Assets

Place source images here. Import them in components and render through Astro's
`<Image />` / `<Picture />` so they're optimized (AVIF/WebP, responsive `srcset`,
correct dimensions to avoid layout shift) at build time.

Use `public/` only for files that must be served as-is (e.g. `favicon.svg`).
